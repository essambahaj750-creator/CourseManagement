# تقرير إعادة تصميم واجهة MVC — منصة المعرفة

## 1. الملخص التنفيذي

تمت إعادة تصميم واجهة MVC في مشروع `CourseManagement.API` لتصبح واجهة عربية RTL متكاملة لمنصة تعليمية باسم **منصة المعرفة**. ركّز التنفيذ على تحويل الواجهة من صفحات CRUD تقليدية إلى تجربة تعليمية واضحة تبدأ من لوحة التحكم، وتنتقل إلى استكشاف الكورسات، ثم التفاصيل والتسجيل، مع فصل واضح بين تجربة الطالب وتجربة المدرب وتجربة المدير.

حافظت إعادة التصميم على بنية ASP.NET Core MVC الحالية، وعلى منطق الأعمال والصلاحيات ومسارات الـAPI، وأضافت فوقها طبقة عرض متماسكة تشمل AppBar وDrawer وBottom Navigation للهاتف، وبطاقات مرئية للكورسات، جداول DataTables، Modals للإجراءات الإدارية، نماذج متحققة، وحالات نجاح وخطأ موحدة.

## 2. مطابقة متطلبات الدكتور

| المتطلب الأكاديمي | التطبيق داخل المشروع | دليل المراجعة |
|---|---|---|
| ثلاثة جداول مترابطة | `Users` و`Courses` و`Enrollments`، حيث يرتبط التسجيل بالمستخدم والكورس ويرتبط الكورس بالمدرب | Entities وEF Core Migrations |
| وجود صور في أحد الجداول | `Courses.ImageUrl`، وتظهر الصورة في شبكة الكورسات وصفحة التفاصيل والجداول | `CourseResponseDto` وViews/Courses |
| Main Dashboard | مؤشرات عدد الكورسات والمستخدمين والتسجيلات، أحدث الكورسات، ونشاط التسجيلات | `Views/Home/Index.cshtml` |
| CRUD كامل | إنشاء وتعديل وحذف وعرض تفاصيل الكورسات، وإدارة المستخدمين والتسجيلات وفق الدور | Views/Courses وViews/Admin |
| Bootstrap | Bootstrap 5.3 RTL مستخدم في التخطيط والنماذج والـGrid والـModal | `_Layout.cshtml` |
| DataTables وModals | جداول عربية قابلة للبحث والترتيب، وModal لتغيير دور المستخدم وتأكيد الإجراءات | `site.js` وViews/Admin |
| Modules وScreenshots وBusiness Logic | توثيق الوحدات والشاشات ومسارات التسجيل والصلاحيات ومنطق الكورس | هذا التقرير و`REDESIGN_PLAN_AR.md` |

## 3. الهوية البصرية وتجربة المستخدم

تم بناء هوية بصرية تعليمية حديثة تعتمد خلفية فاتحة، كحليًا عميقًا للثقة، تركوازيًا للتفاعل، وبطاقات بيضاء ذات حدود وظلال خفيفة. تم تعريف الألوان والمسافات والزوايا والظلال في متغيرات CSS مركزية داخل `wwwroot/css/site.css` حتى يكون التصميم قابلًا للتوسع، بدل تكرار القيم في كل View.

اللغة العربية هي اللغة الأساسية، واتجاه الصفحة RTL مفعّل على مستوى التخطيط. كما تم استخدام خط Cairo مع fallback محلي، وBootstrap Icons لتوحيد لغة الأيقونات. الأزرار الأساسية والإجراءات الثانوية والحالات الخطرة لها تباين واضح، مع تحسين حالة التركيز للحقول والأزرار من أجل تجربة أكثر قابلية للاستخدام.

## 4. الوحدات والشاشات

| الوحدة | الشاشات | الهدف التجاري |
|---|---|---|
| المصادقة والحساب | تسجيل الدخول، إنشاء حساب، تغيير كلمة المرور | إنشاء جلسة آمنة وإدارة بيانات الحساب |
| لوحة التحكم | الصفحة الرئيسية | عرض ملخص فوري للمنصة وروابط العمل السريع |
| استكشاف المحتوى | قائمة الكورسات، تفاصيل الكورس | اكتشاف المحتوى وقراءة الوصف واتخاذ قرار التسجيل |
| إدارة الكورسات | إضافة، تعديل، حذف، تفاصيل | تمكين المدرب والمدير من إدارة المحتوى |
| تسجيلات الطالب | تسجيلاتي، التسجيل، إلغاء التسجيل | متابعة المسار التعليمي للمستخدم |
| الإدارة والرقابة | المستخدمون، كل التسجيلات | تغيير الأدوار ومراجعة العلاقات والنشاط |

## 5. منطق الأعمال والتدفقات الرئيسية

يبدأ الزائر من صفحة الكورسات أو من زر إنشاء الحساب. يستطيع الزائر تصفح الكورسات وقراءة التفاصيل، بينما يتطلب التسجيل في كورس وجود جلسة مصادق عليها. يحق للمدرب إنشاء كورسات وتعديل أو حذف الكورسات التي يملكها، في حين يمتلك المدير صلاحية إدارة جميع الكورسات والمستخدمين والتسجيلات.

يرتبط شريط البحث في الـAppBar بالـ`CoursesController` عبر المعامل `q`. ويجري البحث في عنوان الكورس ووصفه واسم المدرب، ثم يعرض النتائج في شبكة بطاقات وجدول DataTables. هذا يجعل البحث وظيفة Backend حقيقية وليست عنصرًا بصريًا فقط.

تستخدم عمليات الإنشاء والتعديل والحذف حماية `ValidateAntiForgeryToken`، ويُعاد المستخدم إلى صفحة مناسبة بعد نجاح العملية مع رسالة حالة عبر `TempData`. كما يُمنع المدرب من تعديل أو حذف محتوى لا يملكه، بينما يستطيع المدير إدارة كل المحتوى وفق الصلاحيات المعرفة في الـController والخدمات.

## 6. عناصر الـFull‑Stack المنفذة

تم ربط الواجهة بطبقة الخدمات وDTOs الموجودة بدل وضع منطق البيانات داخل Razor Views. يتولى الـController استقبال الطلب والتحقق من الصلاحيات والتحقق من النموذج، ثم تستدعي طبقة Application الخدمة المناسبة، وتنفذ Infrastructure الاستعلامات والتغييرات عبر EF Core. هذا يحافظ على الفصل بين العرض ومنطق الأعمال والبيانات.

يشمل الحل أيضًا JWT للـAPI وCookie للمصادقة في MVC، وPolicy Scheme للتوجيه بينهما، وSecurityStamp لإبطال الجلسات القديمة، وRate Limiting لمسارات المصادقة، وCORS صريحًا، وUser Secrets للأسرار، وProblemDetails للأخطاء المركزية، وHealth Check على `/health`.

## 7. الجودة والأمان والاستجابة

أُعيد بناء المشروع في وضع Release بنجاح دون أخطاء أو تحذيرات. نجحت اختبارات xUnit الحالية وعددها أربعة اختبارات. كما تم تشغيل التطبيق محليًا في وضع Production مع تعطيل Migrations وSeed مؤقتًا، ثم تم التحقق من أن صفحة `/Account/Login` وصفحة `/Account/Register` تعيدان `HTTP 200` وتظهران التصميم الجديد فعليًا.

كان التحقق البصري مفيدًا في اكتشاف مشكلة runtime لم تظهر في البناء: القائمة الجانبية كانت تستخدم `Model.IsAuthenticated` دون تعريف نموذج tuple صريح. تمت معالجة ذلك بإضافة تعريف النموذج المطابق للقيمة التي يمررها `_Layout.cshtml`، ثم أُعيد البناء والاختبار ونجحا.

تمت مراعاة الشاشات الصغيرة عبر Drawer قابل للفتح وBottom Navigation، وإخفاء البحث الواسع في الهاتف، وتحويل الجداول إلى مناطق تمرير، وتقليل أحجام البطاقات والمسافات. كما أن الملفات المتعقبة لا تحتوي أسرارًا فعلية؛ وجود اسم متغير `JwtSettings__Secret` في رسالة الخطأ هو إرشاد تشغيلي فقط وليس قيمة سرية.

## 8. طريقة التشغيل المحلية

للتشغيل في بيئة تطوير Windows التي يتوفر فيها SQL Server LocalDB، يُضبط السر وقاعدة البيانات عبر User Secrets بدل وضعهما في Git:

```bash
dotnet user-secrets set "JwtSettings:Secret" "ضع-سرًا-قويًا-خاصًا-ببيئتك"
dotnet user-secrets set "ConnectionStrings:DefaultConnection" "اتصال-SQL-Server-الخاص-بك"
dotnet run --project CourseManagement.API/CourseManagement.API.csproj
```

بعد التشغيل يمكن فتح `/` للوحة التحكم، و`/Courses` لاستكشاف الكورسات، و`/Account/Login` للمصادقة، و`/health` لفحص صحة التطبيق. في الإنتاج يجب استخدام SQL Server فعلي، وأسرار مخصصة للإنتاج، وتطبيق Migrations من خلال Pipeline أو خطوة نشر مضبوطة بدل تفعيلها عشوائيًا عند الإقلاع.

## 9. الملفات الأهم في إعادة التصميم

| الملف | المسؤولية |
|---|---|
| `Views/Shared/_Layout.cshtml` | AppBar، التخطيط العام، Drawer، Footer، والتنقل السفلي |
| `Views/Shared/_Navigation.cshtml` | القائمة حسب المصادقة والدور والصفحة الحالية |
| `Views/Home/Index.cshtml` | Dashboard والمؤشرات والنشاط الحديث |
| `Views/Courses/Index.cshtml` | شبكة الكورسات وجدول DataTables والبحث |
| `Views/Courses/Details.cshtml` | صفحة قرار التسجيل ومعلومات الكورس |
| `Views/Admin/Users.cshtml` | إدارة المستخدمين وتغيير الأدوار عبر Modal |
| `wwwroot/css/site.css` | نظام التصميم المركزي والاستجابة |
| `wwwroot/js/site.js` | DataTables، التأكيدات، حالة النماذج، وسلوك Drawer |
| `REDESIGN_PLAN_AR.md` | خطة التنفيذ ومعايير القبول |

## 10. التقييم النهائي

النسخة الحالية مناسبة للعرض الأكاديمي في Week 7 لأنها تعرض الوحدات المطلوبة، الشاشات، علاقات البيانات، صور الكورسات، Dashboard، CRUD، Bootstrap، DataTables، Modals، ومنطق الأعمال المرتبط بالصلاحيات والتسجيل. وهي كذلك أساس عملي قابل للتطوير إلى منتج إنتاجي بعد ربطها بقاعدة SQL Server فعلية، ضبط الأسرار، إضافة مراقبة مركزية، وتوسيع اختبارات التكامل الخاصة بالبيانات.
