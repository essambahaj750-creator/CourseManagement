# مراجعة المعمارية والعقود

## التقييم الحالي

المشروع مبني على طبقات منفصلة نسبيًا: `Domain` للكيانات والتعدادات، `Application` للـDTOs والواجهات، `Infrastructure` للتخزين والخدمات، و`API` للـControllers وMVC. هذا التقسيم مناسب كبداية، ووجود واجهات للمستودعات والخدمات يقلل الارتباط المباشر بين الطبقات.

إضافة MVC تمت داخل مشروع الـAPI نفسه، وهو خيار عملي لتطبيق متوسط الحجم، لكنه يتطلب فصلًا واضحًا بين Controllers الخاصة بالـAPI وControllers الخاصة بـMVC، وبين JWT وCookie، حتى لا يصبح اختيار مخطط المصادقة غامضًا.

## الفجوات المؤكدة

| المجال | الملاحظة | الأثر | المعالجة المخططة |
|---|---|---|---|
| الأخطاء | الخدمات ترمي استثناءات عامة مثل `InvalidOperationException` و`KeyNotFoundException` | صعوبة التمييز بين تعارض، تحقق، وعدم وجود، وإمكان إرجاع رمز غير دقيق | إضافة استثناءات تطبيقية مخصصة وProblemDetails موحد |
| المصادقة | كيان المستخدم لا يحتوي `SecurityStamp` أو `TokenVersion` | تغيير الدور أو حذف المستخدم لا يبطل JWT/Cookie القديمة | إضافة إصدار أمان للمستخدم والتحقق منه عند كل مصادقة |
| الأسرار | Seeder يعتمد على إعدادات `SeedAdmin`، وJWT يعتمد على Secret من الإعدادات | خطر تسريب أو استخدام قيمة افتراضية في بيئة غير مناسبة | نقل الأسرار إلى User Secrets/Environment وعدم قبول قيم افتراضية |
| CORS | عند غياب AllowedOrigins أو وجود `*` يتم السماح لكل الأصل | توسيع سطح الهجوم، خصوصًا عند توسعة المصادقة | رفض wildcard في الإنتاج وفرض قائمة origins صريحة |
| الترحيلات | يتم تنفيذ `MigrateAsync` عند كل تشغيل للتطبيق | قد يسبب فشل الإقلاع أو تغييرات غير متوقعة في الإنتاج | جعل الترحيلات خطوة نشر صريحة أو تفعيلها بإعداد واضح |
| MVC | `AccountController` يلتقط `Exception` العامة | إخفاء أخطاء برمجية وإضعاف المراقبة | التقاط استثناءات معروفة وتسجيل المجهول مركزيًا |
| الاختبارات | لا توجد مشاريع اختبارات فعلية | لا يوجد ضمان آلي للصلاحيات أو قواعد التسجيل | إضافة Unit وIntegration Tests للمسارات الحرجة |
| الاستعلامات | بعض المستودعات تعرض قوائم كاملة دون pagination | تدهور الأداء مع نمو البيانات | إضافة pagination وprojection و`AsNoTracking` حيث يلزم |

## القرار المعماري

سيبقى المشروع في حل واحد مع الحفاظ على الطبقات الأربع الحالية. لن تتم إعادة كتابة الـAPI أو إدخال إطار جديد للـMVC. ستُضاف تحسينات صغيرة قابلة للمراجعة: عقود أخطاء، مكونات أمان، اختبارات، إعدادات تشغيل، وتحسينات مستودعات.

سيستمر استخدام JWT لمسارات `/api/*`، وCookie لمسارات MVC، لكن ستتم إدارة معلومات الهوية من مصدر موحد قدر الإمكان، مع Claim للإصدار الأمني وإعادة التحقق من المستخدم عند الحاجة. وستبقى ViewModels مستقلة عن DTOs حتى لا تتحول Views إلى امتداد مباشر لعقود التخزين.

## ترتيب التنفيذ

أولًا تُضاف البنية المشتركة للاستثناءات وProblemDetails لأنها ستُستخدم في API وMVC. ثانيًا تُضاف حقول أمان المستخدم وتهيئة الأسرار. ثالثًا تُحسن طبقة البيانات وعمليات التسجيل. بعد ذلك تُراجع الواجهة وتضاف الاختبارات. وأخيرًا تُضبط إعدادات التشغيل والـCI والتوثيق.

## معايير عدم كسر التوافق

يجب أن تبقى المسارات الحالية مثل `api/Auth/login` و`api/Auth/register` متاحة. ويجب ألا تتغير أسماء خصائص DTO الحالية إلا بإضافة خصائص اختيارية أو إصدار عقد جديد عند الضرورة. أي Migration جديدة يجب أن تكون قابلة للرجوع ولا تقلص أعمدة موجودة دون قرار صريح.

## الحالة

اكتملت مراجعة المعمارية. المرحلة التالية هي تنفيذ تقوية المصادقة والصلاحيات وإدارة الأسرار.
