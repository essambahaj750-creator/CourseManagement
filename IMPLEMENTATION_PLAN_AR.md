# خطة تطوير CourseManagement.API وواجهة MVC

## الهدف العام

رفع المشروع من نسخة MVP تعمل وظيفيًا إلى تطبيق منظم وقابل للصيانة وأكثر أمانًا، مع الحفاظ على عقود الـAPI الحالية قدر الإمكان، وتقديم واجهة ASP.NET Core MVC عربية ومتجاوبة، وإضافة اختبارات وتوثيق وتشغيل قابل للتكرار.

> كل مرحلة تُنفذ على فرع `professional-refactor`، ثم تُبنى وتُراجع قبل الانتقال إلى المرحلة التالية.

## خط الأساس

| البند | الحالة الحالية | معيار النجاح النهائي |
|---|---|---|
| بناء الحل | ناجح | نجاح البناء مع `TreatWarningsAsErrors` أو دون تحذيرات مؤثرة |
| الاختبارات | لا توجد مشاريع اختبارات فعلية | اختبارات وحدة وتكامل للمصادقة والصلاحيات والخدمات الأساسية |
| المعمارية | Domain/Application/Infrastructure/API مع MVC مضافة | عقود واضحة، أخطاء موحدة، وعدم تكرار منطق الأعمال |
| المصادقة | JWT للـAPI وCookie لـMVC | أسرار خارج المستودع، توكنات قابلة للإبطال، Cookie محمية، وRate Limiting |
| قاعدة البيانات | EF Core وSQLite | Migration قابلة للتطبيق، فهارس وقيود واضحة، وملف قاعدة بيانات قابل للنسخ الاحتياطي |
| الواجهة | Razor MVC وBootstrap | تحقق خادمي وعميل، حماية CSRF، صلاحيات واضحة، وتجربة RTL متجاوبة |
| التشغيل | يعتمد على ملف SQLite محلي | إعدادات منفصلة للتطوير والإنتاج وتعليمات تشغيل قابلة للتكرار ونسخ احتياطي |

## مراحل التنفيذ ومعايير القبول

### المرحلة الأولى: خط الأساس والنطاق

يتم تثبيت الفرع، حفظ نتيجة البناء والاختبارات، وحصر الملفات الحساسة والعقود الحالية. لا يُقبل أي تغيير لاحق دون أن يكون قابلًا للتمييز والمراجعة.

### المرحلة الثانية: المعمارية والعقود

يتم توحيد نماذج الأخطاء، فصل استثناءات منطق الأعمال عن أخطاء البنية التحتية، مراجعة رموز HTTP، وتقليل تكرار `try/catch` داخل Controllers. يجب أن تبقى الـDTOs حدودًا واضحة بين الواجهة والمنطق الداخلي.

### المرحلة الثالثة: المصادقة والأمان

يتم نقل الأسرار إلى User Secrets أو متغيرات البيئة، منع قيم Admin الافتراضية، إضافة `SecurityStamp` أو `TokenVersion` للمستخدم، إصدار JWT بمعرف فريد ومدة مناسبة، والتحقق من الإصدار عند كل طلب. يجب أن تؤدي إزالة المستخدم أو تغيير دوره أو تغيير كلمة المرور إلى إبطال الجلسات القديمة. كما تُضاف حماية Cookie وRate Limiting لمسارات الدخول والتسجيل.

### المرحلة الرابعة: البيانات ومنطق الأعمال

يتم إضافة قيود وفهارس قاعدة البيانات، معالجة التعارضات بشكل صريح، تحسين الاستعلامات، واستخدام استثناءات مخصصة. لا يُسمح بتسريب رسائل SQL أو تفاصيل داخلية للعميل، وتكون عمليات التعديل والحذف آمنة ومتحققة من الملكية والصلاحية.

### المرحلة الخامسة: واجهة MVC

يتم توحيد Layout وNavigation، ربط النماذج بـViewModels، استكمال التحقق الخادمي والعميل، حماية كل POST بـAnti-Forgery، منع Open Redirect، ضبط صفحات 401/403/404/500، وتحسين الجداول والنماذج على الهاتف والسطح المكتبي.

### المرحلة السادسة: الجودة والأداء

تُضاف اختبارات للمصادقة والصلاحيات وقواعد التسجيل، وفحوص static analysis و`git diff --check`. تُراجع الاستعلامات و`AsNoTracking` والترقيم، وتُضاف Logging منظم وCorrelation/Trace ID دون تسجيل كلمات المرور أو التوكنات.

### المرحلة السابعة: التشغيل والنشر

يتم فصل إعدادات Development وProduction، إضافة ملف إعداد نموذجي، توثيق الترحيلات، توثيق الأسرار المطلوبة، والتحقق من التشغيل على SQLite محليًا أو في مسار إنتاج دائم مع صلاحيات القراءة والكتابة ونسخ احتياطي مضبوط.

### المرحلة الثامنة: التسليم

يتم تنفيذ build وtest وmigration script وفحص الأسرار والملفات غير المتعقبة، ثم تسجيل التغييرات في Git ورفع الفرع النهائي مع تقرير يوضح ما تم وما بقي وما يلزم لتشغيله في بيئة حقيقية.

## قواعد التنفيذ

لن تُحذف عقود API الحالية دون سبب موثق. أي تغيير في قاعدة البيانات سيكون عبر Migration. لن تُخزن أسرار حقيقية أو كلمات مرور افتراضية في Git. وسيتم تشغيل فحص البناء بعد كل مجموعة تعديلات مترابطة، مع تسجيل نتيجة كل مرحلة في Git history.

## تعريف الانتهاء

تُعد النسخة جاهزة للتسليم عندما ينجح البناء، تمر الاختبارات المضافة، تكون إعدادات الإنتاج خالية من الأسرار المضمنة، تكون الترحيلات قابلة للتوليد، وتكون مسارات الـAPI وMVC محمية ومذكورة في README مع خطوات تشغيل واضحة.

## المؤلف

Manus AI

## المراجع

لا تعتمد هذه الخطة على مصدر خارجي؛ وهي مبنية على فحص بنية المستودع وملفات المشروع الحالية.

---

## سجل التنفيذ

| المرحلة | الحالة | الملاحظات |
|---|---|---|
| 1. خط الأساس | قيد التنفيذ | البناء ناجح، ولا توجد مشاريع اختبارات فعلية حاليًا. |
| 2. المعمارية والعقود | لم تبدأ | ستبدأ بعد تثبيت هذا المستند. |
| 3. المصادقة والأمان | لم تبدأ | ستُنفذ بعد مراجعة العقود. |
| 4. البيانات ومنطق الأعمال | لم تبدأ | ستُنفذ مع Migration عند الحاجة. |
| 5. واجهة MVC | منفذة جزئيًا | ستُراجع وتُحسن بدل إعادة بنائها من الصفر. |
| 6. الجودة والأداء | لم تبدأ | ستتضمن إضافة مشاريع اختبارات. |
| 7. التشغيل والنشر | منفذة | توثيق SQLite والأسرار ومسار ملف قاعدة البيانات. |
| 8. التسليم | منفذة | تم رفع النسخة وإرفاق التوثيق وحزمة التشغيل. |
