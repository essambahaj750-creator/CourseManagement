# سجل نتائج التدقيق الشامل — CourseManagement

**تاريخ التدقيق:** 26–27 أغسطس 2026
**الفرع:** `professional-refactor`
**نطاق التدقيق:** ASP.NET Core API + ASP.NET Core MVC + Flutter + EF Core/SQLite

---

## الملخص التنفيذي

تم تنفيذ تدقيق كامل على ست مراحل (اكتشاف، معمارية، أمان، بيانات، وظائف، Flutter). النتيجة العامة: **المشروع أقوى بكثير مما يبدو من حجمه** — لا توجد ثغرات حقن (SQL/XSS/Command/SSRF)، والحماية من CSRF مفعّلة على جميع نماذج MVC الثمانية عشر، وصلاحيات الملفات مفروضة على مستوى الخدمة، ورفع الملفات حقيقي من الجهاز وليس وهميًا.

لكن وُجدت مشكلتان حرجتان (تم إصلاحهما) و**14 مشكلة عالية الخطورة** ما زالت قائمة، أخطرها أن سلسلة رسائل الأخطاء تُخفي سبب فشل أي عملية رفع عن المستخدم وعن الاختبارات معًا.

### ما تم إصلاحه والتحقق منه

| # | المشكلة | الإصلاح | التحقق |
|---|---|---|---|
| C1 | مفتاح JWT وكلمة مرور المدير موجودان في تاريخ Git | تدوير المفتاح (88 حرفًا) وكلمتي المرور عبر User Secrets، وتحديث بصمة الحسابين في قاعدتي البيانات | `newPasswordAccepted=True`, `securityStampRotated=True`, `integrity_check=ok` |
| C2 | البريد الإلكتروني بحروف كبيرة يمنع صاحبه من الدخول نهائيًا | `User.NormalizeEmail` + `COLLATE NOCASE` على العمود + Migration | 26/26 اختبار ناجح، وأُثبت فشلها قبل الإصلاح |

> **تنبيه أمني:** القيم القديمة لمفتاح JWT وكلمة مرور المدير تبقى قابلة للقراءة في تاريخ Git. تم اعتمادها كمحروقة (burned) ولا يجب استخدامها مرة أخرى. المستودع خاص (private)، وقرار المالك كان التدوير دون إعادة كتابة التاريخ.

---

## دليل مستويات الخطورة وحالة التحقق

**الخطورة:** 🔴 حرجة · 🟠 عالية · 🟡 متوسطة · 🟢 منخفضة

**حالة التحقق** — مهمة لأن التقرير يفصل بين ما أُثبت وما استُنتج:

| الحالة | المعنى |
|---|---|
| `أُثبت بأمر` | نُفِّذ أمر فعلي ونتيجته مسجّلة حرفيًا |
| `أُثبت بقراءة كود` | مستنتج من الكود المصدري مباشرة، بمسار وسطر محدد |
| `يحتاج تحقق تشغيلي` | يحتاج تشغيل التطبيق أو محاكي Android للتأكيد النهائي |
| `مُصلَح ومُختبَر` | أُصلح ويوجد اختبار انحدار يفشل بدون الإصلاح |

---

## البيئة ونتائج القياس الأساسي

| الأداة | الإصدار |
|---|---|
| .NET SDK | `10.0.101` (ASP.NET Core Runtime 10.0.1) |
| Flutter | `3.35.7` stable |
| Dart | `3.9.2` |

| الأمر | النتيجة |
|---|---|
| `dotnet build CourseManagement.API.slnx` | ❌ فشل — قفل ملفات بسبب تشغيل `CourseManagement.Web.exe` (ليس خطأ كود) |
| `dotnet build --artifacts-path <temp>` | ✅ `0 Warning(s), 0 Error(s)` |
| `dotnet test` | ✅ `Failed: 0, Passed: 26, Total: 26` |
| `flutter analyze` | ✅ `No issues found!` |
| `flutter test` | ✅ `4/4 passed` |
| `flutter build web --release` | ✅ `✓ Built build\web` |
| `dotnet ef migrations list` | ✅ 3 migrations، تُطبَّق من صفر بنجاح |
| `dotnet format --verify-no-changes` | ⚠️ لم يُنفَّذ — لا يوجد `.editorconfig` |

---

## نقاط القوة المؤكدة (يجب الحفاظ عليها)

هذه ليست مجاملات — كل بند تم التحقق منه بقراءة الكود أو بأمر:

- **لا توجد ثغرات حقن على الإطلاق.** فحص شامل لكل الملفات المتتبَّعة: صفر `FromSqlRaw`/`ExecuteSqlRaw`، صفر `Html.Raw`، صفر `Process.Start`، صفر `BinaryFormatter`/`XmlSerializer`، وصفر `HttpClient` على الخادم (أي لا سطح SSRF).
- **حماية CSRF كاملة** على جميع الـ 18 عملية POST في MVC (تم عدّها يدويًا).
- **إبطال الجلسة فوريًا** عبر `SecurityStamp` يُتحقق منه في *كل* طلب مُوثَّق، في الـ API والـ MVC، ويفشل بشكل مغلق (fail-closed).
- **منع Open Redirect** عبر `Url.IsLocalUrl`.
- **منع Mass Assignment**: `CourseDto` لا يحتوي `InstructorId` ولا `ImageUrl`، و`RegisterDto` لا يحتوي `Role`، والإنشاء يفرض المالك من التوكن.
- **رفع الملفات آمن في جوهره**: أسماء ملفات من توليد الخادم (`Guid`)، منع Path Traversal مُختبَر، تخزين خارج `wwwroot`، وتنظيف الملفات اليتيمة عند فشل حفظ البيانات.
- **إصلاح IDOR** على نقاط التسجيلات والمستخدمين.
- **CORS يفشل مغلقًا** عند الأصول الفارغة أو `*`، ولا يُدمج مع `AllowCredentials`.
- **ترقيم صفحات مستقر**: كل ترتيب لديه `ThenByDescending(Id)` فلا تتكرر أو تُفقد صفوف بين الصفحات.
- **WAL مفعّل** على قاعدتي البيانات، والهجرات محمية بـ `__EFMigrationsLock`.
- **الهجرة التلقائية معطّلة في الإنتاج** افتراضيًا (`ApplyMigrations: false` في `appsettings.json`).
- **رفع محلي حقيقي في Flutter** مع قوائم امتدادات مطابقة تمامًا للخادم، و**معاينة الكورس قبل الحفظ** مُنفَّذة فعليًا، و**بث فيديو مُوثَّق** يعمل بترويسة `Authorization` مع دعم `Range` من الخادم.

---

## السجل الرئيسي — المشاكل عالية الخطورة (14 مشكلة قائمة)

### عقد الأخطاء

#### 🟠 SEC-S1 · تسريب رسائل داخلية + تعداد الحسابات
`CourseManagement.API/Middleware/ExceptionHandlingMiddleware.cs:76-79` (ونظيره في Web)
`InvalidOperationException` تُعاد بحالة 409 مع `invalidOperation.Message` **حرفيًا**. النتيجة: تسريب تفاصيل إعداد (مثل `"JwtSettings:Secret is not configured."`) لمستخدم مجهول، و**تعداد حسابات** عبر `"User with this email already exists."`.
**الإصلاح:** استخدام استثناء `ConflictException` يحمل رسائل مُعتمدة فقط. **اختبار:** التأكد أن نص خطأ الإعداد لا يظهر في جسم الاستجابة.
`أُثبت بقراءة كود`

#### 🟠 SEC-S8 · إخفاء سبب فشل الرفع عن المستخدم والاختبارات
`ExceptionHandlingMiddleware.cs:68-71` (كلا التطبيقين)
`ArgumentException` تُحوَّل إلى 400 مع **إسقاط الرسالة الحقيقية** واستبدالها بـ `"One or more request values are invalid."`. كل رسائل التحقق المكتوبة بعناية (`"The file exceeds the 50 MB limit."`، `"This video extension is not allowed."`) لا تصل أبدًا. عميل Flutter يقرأ الحقل `detail` تحديدًا، أي أنه يعرض الرسالة العامة.
**هذه أهم مشكلة في السجل** لأنها تُعمي المستخدم *والاختبارات* عن سبب أي فشل.
`أُثبت بقراءة كود`

#### 🟠 FUNC-F6 · تسريب `ex.Message` الخام إلى واجهة MVC في 11 موضعًا
`AdminController` (7)، `EnrollmentsController` (2)، `CoursesController` (2). هذا تحايل على SEC-S8، لكنه يسرّب نصًا داخليًا للمستخدم.
`أُثبت بقراءة كود`

### رفع الملفات

#### 🟠 SEC-S13 · تجاوز كامل للتحقق من نوع المحتوى + غياب فحص البصمة
`CourseManagement.Infrastructure/Services/CourseAssetService.cs:274` و`:297`
كلتا الدالتين تُعيدان `true` فورًا إذا كان نوع المحتوى `application/octet-stream` — وهي قيمة **يتحكم بها العميل بالكامل**. أي أن التحقق من MIME **لا يقدم أي قيمة أمنية**. ولا يوجد **أي** فحص للبصمة الرقمية (magic bytes) في المشروع، وهو مطلب صريح في التدقيق. القائمة البيضاء للامتدادات هي البوابة الحقيقية الوحيدة.
**الإصلاح:** إزالة تجاوز `octet-stream`، وإضافة فحص بصمة (JPEG/PNG/WebP/MP4/PDF/ZIP)، وتخزين نوع محتوى **مُستنتج من الخادم** لا من العميل.
`أُثبت بقراءة كود`

#### 🟠 SEC-S14 · استبدال الغلاف غير ذرّي — يُفسد الغلاف نهائيًا
`CourseAssetService.cs:127-175` مع `CourseAssetRepository.cs:28`
`AddAsync` تُنفِّذ `SaveChanges` فورًا. إذا فشل `UpdateAsync(course)` بعدها، فإن `catch` يحذف **الملف الجديد** لكن يترك **صف البيانات مُثبَّتًا**، والصف القديم محذوف أصلًا. النتيجة: غلاف مكسور نهائيًا (`OpenCoverAsync` تُرجع "الغلاف المخزَّن مفقود"). لا توجد أي معاملة (transaction) في المشروع كله.
`أُثبت بقراءة كود`

#### 🟠 SEC-S15 · حد حجم 512MB مقابل سقف 50MB + لا تحديد معدل
`CourseAssetsController.cs:31` و`CoursesController.cs:79`
`RequestSizeLimit(512MB)` على رفع المرفقات التي سقفها 50MB → يُخزَّن 512MB على القرص قبل الرفض. ولا يوجد `EnableRateLimiting` على **أي** نقطة رفع (التحديد مطبَّق على المصادقة فقط).
`أُثبت بقراءة كود`

### البيانات

#### 🟠 DATA-D1 · فقدان UTC عبر السلسلة كاملة — والعميلان يخطئان في اتجاهين متعاكسين
SQLite يخزّن `DateTime` بصيغة `2026-08-26 13:50:41.9517705` — **بدون `Z` وبدون إزاحة**، فيُفقد `DateTimeKind` عند القراءة ويصبح `Unspecified`، و`System.Text.Json` يُخرجه بدون `Z`.
ثم يخمّن Flutter، **وبشكل غير متسق**: `Enrollment.enrolledDate` تستخدم `.toLocal()` (تحليل كمحلي ← بلا أثر) بينما `CourseAsset.createdAtUtc` تستخدم `.toUtc()` (تحليل كمحلي ← إزاحة). على جهاز بتوقيت UTC+3 **القيمتان خاطئتان بثلاث ساعات في اتجاهين متعاكسين**. (`AuthSession.expiresAtUtc` سليمة فقط لأنها تُحسب في الذاكرة وتحفظ `Kind=Utc`.)
`أُثبت بأمر` — عبر استعلام مباشر على قاعدتي البيانات

### التوافق بين التطبيقات

#### 🟠 FUNC-H3 · صور الأغلفة مكسورة في MVC
`CourseAssetService.cs:152` تكتب `ImageUrl = "/api/course/{courseId}/cover"` بشكل ثابت، لكن MVC يخدم الأغلفة على `/Courses/Details/{id}/Cover`. القوالب تعرض `<img src="@course.ImageUrl">` في `Home/Index.cshtml:84` و`Courses/Index.cshtml:216` و`Courses/Details.cshtml:25` → **404 على كل غلاف مرفوع من MVC**. (عميل Flutter سليم لأنه يحلّ المسار مقابل `API_BASE_URL`.)
`أُثبت بقراءة كود`

#### 🟠 FUNC-F1 · حذف الغلاف مستحيل
`GetByCourse` تستبعد `CoureImage` من القائمة، فلا يستطيع أي عميل معرفة `assetId` الخاص بالغلاف — و`Delete` تتطلبه. ولا شيء يُفرِّغ `Course.ImageUrl`. النتيجة: يمكن **استبدال** الغلاف فقط، لا حذفه. واجهة Flutter تؤكد ذلك نصًا: «اتركه فارغًا للحفاظ على الغلاف الحالي».
`أُثبت بقراءة كود`

#### 🟠 FUNC-F2 · جداول الإدارة تستخدم واجهة مكتبة أخرى
`wwwroot/js/site.js:13-25` يمرّر `searchable`/`sortable`/`perPage`/`perPageSelect`/`labels` — وهذه خيارات **simple-datatables**، لكن `_Layout.cshtml:139` يُحمّل **DataTables.net 2.1.8** التي تستخدم `searching`/`ordering`/`pageLength`/`language`. النتيجة: الجداول تتجاهل الإعداد وتظهر بواجهة **إنجليزية** افتراضية، بما يخالف متطلب التعريب.
`أُثبت بقراءة كود`

### Flutter

#### 🟠 FLU-FL1 · محاكي Android لا يعمل بأي إعداد جاهز
ثلاث مشاكل متضافرة:
1. `API_BASE_URL` ثابت وقت **الترجمة** (`String.fromEnvironment`) وافتراضه `https://localhost:7026` — بلا تبديل وقت التشغيل وبلا `10.0.2.2`.
2. `AndroidManifest.xml` لا يحتوي `usesCleartextTraffic` ولا `network_security_config.xml` → Android 9+ يحجب `http://10.0.2.2:5205`.
3. `https://10.0.2.2:7026` يفشل في TLS لأن شهادة تطوير ASP.NET موقّعة ذاتيًا وغير موثوقة على المحاكي.
`أُثبت بقراءة كود` + `يحتاج تحقق تشغيلي`

#### 🟠 FLU-FL2 · رفع الملفات ينهار على الفيديوهات الكبيرة
`admin_courses_page.dart:527` — `_pickAndUpload` تستخدم `withData: true` ثم `MultipartFile.fromBytes`، **بلا أي تحقق من الحجم**. ملف 2GB يُحمَّل كاملًا في الذاكرة ثم يُضاعف → **انهيار OOM قبل إرسال أي طلب**. للمقارنة: `_pickCover` *تتحقق* من 5MB.
`أُثبت بقراءة كود`

#### 🟠 FLU-FL3 · لا معالجة لـ 401 في التطبيق كله
المشروع بأكمله يحتوي **فحص حالة واحدًا فقط**: `course_details_page.dart:52` للحالة 403. و`logout()` تُنادى من نقرتين صريحتين فقط. أي إبطال من جهة الخادم — تغيير كلمة مرور، تغيير دور، تعطيل حساب، أو **تدوير مفتاح JWT الذي نُفِّذ في هذا التدقيق** — يترك التطبيق «مُوثَّقًا» ظاهريًا مع فشل كل شاشة وبلا طريق للعودة لتسجيل الدخول.
`أُثبت بأمر` — فحص شامل أظهر فحص حالة واحدًا

#### 🟠 FLU-FL4 · الخط المعلَن غير مُضمَّن — كل الطباعة معطّلة
`app_theme.dart:27` يعلن `fontFamily: 'Cairo'`، لكن: صفر ملفات خطوط في المشروع، ولا قسم `fonts:` في `pubspec.yaml`، ولا `@font-face` في `web/index.html`. التطبيق يرتد صامتًا إلى خط النظام على **كل** المنصات، أي أن هوية الطباعة المصمَّمة غير مفعّلة إطلاقًا.
`أُثبت بأمر`

### البنية التحتية

#### 🟠 OPS-H9 · التكامل المستمر لا يحمي شيئًا
`.github/workflows/ci.yml` يثبّت Flutter `3.47.1` بينما البيئة المحلية `3.35.7` (والاعتماديات مثبَّتة عمدًا لـ 3.35)، و`pull_request.branches` مضبوط على `main` بينما الفرع الافتراضي هو **`master`**. النتيجة: **لا يُفحص أي Pull Request** إلى الفرع الرئيسي، ونتائج CI لا تمثّل البيئة المحلية.
`أُثبت بقراءة كود`

#### 🟠 TEST-M1 · لا توجد اختبارات على مستوى HTTP إطلاقًا
`Microsoft.AspNetCore.Mvc.Testing` مُضاف و`public partial class Program;` موجود في التطبيقين — لكن **بلا استخدام**. ومشروع الاختبارات يشير إلى `CourseManagement.Web` فقط، أي أن **مشروع الـ API لا يملك أي اختبار**: مصادقة JWT، تحديد المعدل، CORS، والوسيط الاستثنائي كلها غير مُختبَرة.
`أُثبت بقراءة كود`

---

## المشاكل المتوسطة (ملخّص بحسب المجال)

| المجال | المعرّفات | الجوهر |
|---|---|---|
| المصادقة | SEC-S2 … S5 | قناة توقيت في تسجيل الدخول تكشف وجود الحساب (BCrypt لا تُنفَّذ إن لم يوجد المستخدم) · سياسة كلمات مرور متناقضة (8 عند التسجيل، 12 عند التغيير) وبلا أي شرط تعقيد · لا قفل حساب، والتحديد 10/دقيقة **لكل IP** فقط، و`me/password` بلا تحديد إطلاقًا · **عمر كوكي MVC يُحكَم فعليًا بـ `JwtSettings:ExpiryHours`** عبر `ExpiresUtc` فيُلغي `ExpireTimeSpan=8h` ويُبطل `SlidingExpiration` |
| ترويسات الأمان | SEC-S10 | لا `X-Content-Type-Options: nosniff`، لا CSP، لا `X-Frame-Options`، لا `Referrer-Policy`، ولا `UseHsts()` — والأغلفة تُخدَم **inline** بنوع محتوى يتأثر بالعميل |
| سلسلة التوريد | FUNC-F5 | **خمسة سكربتات خارجية بلا `integrity`** (jQuery، jquery-validate، unobtrusive، Bootstrap، DataTables) تُحمَّل على صفحات إدارية مُوثَّقة، وبلا CSP يقيّدها |
| الرفع | SEC-S16, S17 | لا حصة تخزين (استنزاف قرص من مستخدم مُوثَّق) · حذف الأصل يُثبِّت الصف ثم يحذف الملف بلا `try/catch` |
| البيانات | DATA-D2 … D5 | لا رمز تزامن (`context.Update` على كيانات مفصولة يكتب كل الأعمدة → **فقدان تحديثات**) · لا إجراء نسخ احتياطي، والنسخ الساذج غير آمن مع WAL، ويجب أن يشمل القاعدة **و**`CourseManagement.Uploads/` معًا · **قاعدتا بيانات متباعدتان** بمجلد رفع واحد مشترك · لا اختبار يثبت عمل Cascade/Restrict |
| المعمارية | ARCH | كل منطق العمل داخل **Infrastructure** لا Application · التجريدات موزَّعة على ثلاثة أماكن · لا `IUnitOfWork` (وهو سبب SEC-S14 المباشر) · `AuthService` يقرأ `IConfiguration` بنصوص سحرية · **كود مشترك مُكرَّر بين API وWeb وقد تباعد فعلًا**: `GetUserId()` يرمي استثناءً في API ويُعيد `0` في Web |
| الأداء | — | `GetInstructorOptionsAsync` تُحمّل **كل** صفوف الكورسات وتجمّعها في الذاكرة على نقطة **عامة** · نقاط غير مُرقَّمة: `GET /api/course`، `GET /api/enrollment`، `GET /api/users` · بحث SQLite **حسّاس لحالة الأحرف** (`instr`) |
| الوظائف | FUNC-F3, F4, F7 | قفل الإرسال المزدوج يُعاد تمكينه بعد **8 ثوانٍ ثابتة** فيسمح برفع مكرر لفيديو كبير · القفل نفسه يعمل حتى عندما يمنع التحقق الإرسال (spinner على نموذج لم يُرسَل) · **لا توجد آلية نشر (publish) في المشروع إطلاقًا** |
| Flutter | FLU-FL5 … FL15 | قائمة إدارة الكورسات مقصورة على **24** بلا ترقيم (تطلب 100 والخادم يقصّها) · قوائم الفلاتر المنسدلة تتباعد عن الحالة بعد «مسح» (`initialValue` يُطبَّق مرة واحدة) · **تباين `#718099` على `#F4F7FC` = 3.73:1** أقل من عتبة WCAG AA (4.5:1) على نص 10–12px، وزرّان بارتفاع 42px تحت حد 48px · تعريب ناقص: لا `localizationsDelegates` ولا `flutter_localizations` → عناصر Material بالإنجليزية · لا تقدّم رفع ولا إلغاء · Flutter يترك كورسًا يتيمًا عند فشل الغلاف بينما MVC يعوّض بالحذف · حالة تحميل مزدوجة في `courses_page` تُنتج استثناءً غير مُعالَج · `intl` مُعلَن وغير مستخدم · تشغيل الفيديو بلا شريط تقدّم |

---

## المشاكل المنخفضة

`register` يُعيد 200 لا 201 · `CreatedAtAction` يشير إلى القائمة لا العنصر · المدير لا يستطيع إنشاء مدرّب مباشرة (خطوتان) · `EditEnrollment` يفقد حالة النموذج عند الخطأ بخلاف `CreateEnrollment` · رمزان مكرران لـ AntiForgery في `Details.cshtml` · عقد التعداد كرقم غير مثبَّت (Flutter يرمّز `1`/`2` يدويًا) · لا فهرس على `Courses.Title` أو `Courses.Price` · لا قيد `CHECK (Price >= 0)` · `FullName` نص غير محدود · لا `Course.CreatedAtUtc` (الترتيب «الأحدث» يستخدم `Id` كبديل) · **`Course.cs` مُخزَّن بترميز Windows-1256 وليس UTF-8** فيُفسد تعليقًا عربيًا · لا `.editorconfig` · لا سياسة تفويض احتياطية (أي متحكم جديد بلا `[Authorize]` يصبح عامًا صامتًا) · ترقيم Flutter يعرض 5 صفحات كحد أقصى · لا سمة داكنة · بطاقات زخرفية بعرض ثابت 250px تستهلك شاشة الهاتف · **لا `Semantics` ولا `semanticLabel` في التطبيق كله** · لا `GoRouter.errorBuilder` · مهلة `restore()` 3 ثوانٍ قد تُخرج المستخدم عند بدء بارد

---

## سلسلة واحدة تشرح لماذا تبدأ من عقد الأخطاء

منتقي الملفات في Flutter يعرض امتدادات الفيديو **والمرفقات** في نافذة واحدة، بينما النوع يُختار من قائمة منفصلة. اختر `type = video` مع ملف `.pdf`: الخادم يرفض بشكل صحيح — لكن **SEC-S8** يُسقط الرسالة، فيقرأ Flutter الحقل `detail` ويعرض *«One or more request values are invalid.»*. المستخدم يقع في خطأ شائع جدًا ولا يحصل على أي معلومة قابلة للتنفيذ.

---

## ترتيب المعالجة الموصى به

كل خطوة تجعل التالية قابلة للتحقق:

1. **ثقة CI (OPS-H9) + هيكل الاختبارات (TEST-M1)** — قبل أن يعمل CI على `master` بالإصدار الصحيح وتوجد اختبارات على مستوى HTTP، لا يمكن إثبات سلامة أي إصلاح.
2. **عقد الأخطاء** (SEC-S1، SEC-S8، FUNC-F6) — لأنه يُخفي حاليًا كل فشل آخر عن المستخدمين والاختبارات.
3. **بوابة الرفع** (SEC-S13، S14، S15، S17 + الحد الاحتياطي في FLU-FL2).
4. **DATA-D1** (UTC) و**FUNC-H3/F1** (دورة حياة الغلاف) — رخيصة، ومرئية للمستخدم، وتحتاج اختبارات عقد.
5. **Flutter**: FLU-FL1، FL3، FL4 — الثلاثة التي تُعطِّل أو تُضعف العميل بالكامل.
6. تقوية المصادقة (SEC-S2…S5)، ثم البيانات (DATA-D2/D6)، ثم الوصولية والواجهة (FLU-FL7/FL8) وبقية المتوسطات.

---

## حدود هذا التدقيق

| البند | الحالة |
|---|---|
| بناء واختبار .NET وFlutter | ✅ نُفِّذ ونتائجه الحرفية مسجَّلة أعلاه |
| الهجرة على نسخة من بيانات حقيقية | ✅ نُفِّذت صعودًا وهبوطًا بلا فقدان صفوف |
| فحص الحقن والصلاحيات والعقود | ✅ بقراءة الكود مع مسارات وأسطر محددة |
| رفع/تنزيل فعلي عبر HTTP | ❌ لم يُنفَّذ — يحتاج تشغيل الخادم |
| محاكي Android والتشغيل على جهاز | ❌ لم يُنفَّذ |
| تشغيل الفيديو وتخطيط RTL بصريًا | ❌ لم يُعايَن بصريًا |
| حالة CI على GitHub | ❌ لم يُتحقق منها |

**ما لم يُنفَّذ لم يُدَّعَ نجاحه.** كل بند في الجدول أعلاه مُعلَّم بصراحة.
